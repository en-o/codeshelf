# CodeShelf 项目搭建计划

## 项目概述
代码书架 - 本地项目管理工具，基于 Tauri + React 构建的桌面应用。

---

## 第一阶段：项目初始化 ✅ 已完成

### 1.1 环境检查
- [x] 检查 Rust 环境（rustc, cargo）
- [x] 检查 Node.js 环境（node, npm/pnpm）
- [x] 安装 Tauri CLI

### 1.2 创建 Tauri 项目
- [x] 手动初始化项目（npm + cargo tauri init）
- [x] 配置 React + TypeScript
- [x] 配置项目名称为 `codeshelf`

### 1.3 前端依赖安装
- [x] Tailwind CSS v4
- [x] Zustand（状态管理）
- [x] @tanstack/react-query（数据请求）
- [x] simple-git（Git 操作）
- [x] lucide-react（图标库）
- [x] date-fns（日期处理）

### 1.4 Tauri 插件配置
- [x] tauri-plugin-sql（SQLite 数据库）
- [x] tauri-plugin-fs（文件系统访问）
- [x] tauri-plugin-dialog（文件选择对话框）
- [x] tauri-plugin-shell（打开外部程序）

---

## 第二阶段：项目结构搭建 ✅ 已完成

### 2.1 前端目录结构
```
src/
├── components/          # 通用组件
│   ├── ui/             # 基础 UI 组件 ✅
│   ├── layout/         # 布局组件 ✅
│   └── project/        # 项目相关组件 ✅
├── pages/              # 页面组件
│   ├── Shelf/          # 项目书架页 ✅
│   ├── Dashboard/      # 仪表盘页 ✅
│   └── Settings/       # 设置页 ✅
├── hooks/              # 自定义 Hooks
├── stores/             # Zustand 状态管理 ✅
├── services/           # API 服务层
│   ├── git/            # Git 操作服务 ✅
│   └── db/             # 数据库服务 ✅
├── types/              # TypeScript 类型定义 ✅
├── utils/              # 工具函数
└── styles/             # 全局样式 ✅
```

### 2.2 Rust 后端结构
```
src-tauri/
├── src/
│   ├── main.rs         # 入口文件 ✅
│   ├── lib.rs          # 库文件 ✅
│   ├── commands/       # Tauri Commands
│   │   ├── mod.rs      ✅
│   │   ├── git.rs      # Git 相关命令 ✅
│   │   ├── project.rs  # 项目管理命令 ✅
│   │   └── system.rs   # 系统命令 ✅
│   └── models/         # 数据模型
└── Cargo.toml          ✅
```

---

## 第三阶段：核心功能实现 ✅ 已完成

### 3.1 数据库设计（使用内存存储，后续可迁移到 SQLite）
- [x] Project 数据模型
- [x] 增删改查操作

### 3.2 Git 命令封装（Tauri Commands）
- [x] `scan_directory` - 扫描目录查找 Git 仓库
- [x] `get_git_status` - 获取仓库状态
- [x] `get_commit_history` - 获取提交历史
- [x] `get_branches` - 获取分支列表
- [x] `get_remotes` - 获取远程仓库
- [x] `add_remote` - 添加远程仓库
- [x] `remove_remote` - 删除远程仓库
- [x] `git_push` - 推送到远程
- [x] `git_pull` - 拉取更新
- [x] `git_fetch` - 获取远程更新

### 3.3 页面组件开发
- [x] 侧边栏导航
- [x] 项目卡片组件
- [x] 搜索筛选组件
- [ ] 目录树组件（待完善）
- [ ] Git 热力图组件（待完善）
- [x] 仪表盘统计组件

---

## 第四阶段：功能完善 🔄 进行中

### 4.1 项目书架功能
- [x] 网格/列表视图切换
- [x] 项目搜索与筛选
- [ ] 标签管理
- [x] 收藏功能
- [ ] 右键菜单操作

### 4.2 Git 统计功能
- [ ] 提交热力图渲染
- [ ] 统计指标计算
- [ ] 分支可视化

### 4.3 远程仓库管理
- [ ] 远程状态检测
- [ ] 强制推送功能
- [ ] 分支同步功能
- [ ] 仓库迁移功能

---

## 运行项目

### 1. 安装系统依赖（WSL/Linux）

```bash
sudo apt update
sudo apt install -y pkg-config libgtk-3-dev libwebkit2gtk-4.1-dev \
  libjavascriptcoregtk-4.1-dev libsoup-3.0-dev libappindicator3-dev librsvg2-dev
```

### 2. 启动开发服务器

```bash
# 前端开发（仅前端）
npm run dev

# Tauri 开发（完整应用）
npm run tauri dev
```

### 3. 构建生产版本

```bash
npm run tauri build
```

---

## 项目结构概览

```
codeshelf/
├── src/                    # 前端源码
│   ├── components/         # React 组件
│   ├── pages/             # 页面组件
│   ├── stores/            # Zustand 状态管理
│   ├── services/          # API 服务
│   ├── types/             # TypeScript 类型
│   └── styles/            # 样式文件
├── src-tauri/             # Tauri/Rust 后端
│   ├── src/
│   │   ├── commands/      # Tauri Commands
│   │   ├── lib.rs         # 主库文件
│   │   └── main.rs        # 入口文件
│   ├── Cargo.toml         # Rust 依赖
│   └── tauri.conf.json    # Tauri 配置
├── package.json           # Node.js 依赖
├── vite.config.ts         # Vite 配置
├── tsconfig.json          # TypeScript 配置
└── PLAN.md               # 本计划文件
```

---

## 预期产出

- ✅ 可运行的 Tauri 桌面应用框架
- ✅ 项目书架管理基础功能
- ✅ Git 仓库状态监控命令
- 🔄 完整的远程仓库管理能力（进行中）
